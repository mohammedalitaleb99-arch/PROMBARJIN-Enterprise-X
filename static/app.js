const chat=document.getElementById('chat'), input=document.getElementById('input'), send=document.getElementById('send'), profile=document.getElementById('profile');
function add(role,text){const d=document.createElement('div');d.className='msg '+role;d.textContent=text;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
async function exec(){const msg=input.value.trim();if(!msg)return;add('user',msg);input.value='';send.disabled=true;send.textContent='RUNNING';try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:msg})});const j=await r.json();add('assistant',j.reply);profile.textContent=JSON.stringify({profile:j.profile,quality_gate:j.quality_gate},null,2)}catch(e){add('assistant','Execution error: '+e)}finally{send.disabled=false;send.textContent='EXECUTE'}}send.onclick=exec;input.addEventListener('keydown',e=>{if(e.key==='Enter'&&(e.ctrlKey||e.metaKey))exec()});
fetch('/health').then(()=>{});
